import React, { FC } from 'react';

import { Container } from './styles';

const Card: FC = () => {
  return (
    <Container>
      <p>Starting a card !!</p>
    </Container>
  );
};

export default Card;
